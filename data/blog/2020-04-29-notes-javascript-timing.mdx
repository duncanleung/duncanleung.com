---
title: 'Notes on Javascript Function Invocation and "this'
date: '2020-04-27'
tags: ['interview-prep', 'javascript']
summary: 'DRAFT: Notes on Javascript Function Invocation and "this'
images: []
draft: true
---

## Implement debounce

```javascript
function debounce(fn, delay) {
  let setTimeoutId = null;

  return function() {
    if (setTimeoutId) {
      clearTimeout(setTimeoutId);
    }

    const setTimeoutId = setTimeout(() => {
      fn.call(this);

      setTimeoutId = null;
    }, delay);
  };
}

const debouncedFn = debounce(() => {}, 500);

// When an action is happening, keep pushing back the start time
// When there is a delay for x seconds, then fire the function
```
